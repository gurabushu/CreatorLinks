# 動作テスト進捗 2026-08-08

対象: creatorLinks 既存コード（決済まわりを除く）
実行者: Claude Code (opus-4-7)
実行環境: macOS / Node / pnpm, Docker postgres (5433) 稼働中

## 概要

「サイトの体裁は取れているか」を検証するため、既存コードに対して以下 5 種のテストを実行する。

| # | テスト種別 | ツール | 目的 |
|---|---|---|---|
| 1 | 型検査 | `tsc --noEmit` (web + api) | 型エラー・シグネチャ不整合の検出 |
| 2 | Lint | `next lint` (web) | コーディング規約違反の検出 |
| 3 | ユニットテスト | vitest (api) | API ルーターのロジック検証 |
| 4 | ビルド | `next build` (web) | 本番ビルド通過確認（暗黙のエラー検出） |
| 5 | E2E | Playwright (web) | ブラウザ経由の主要導線検証 |

## 進捗サマリ

| # | テスト | 初回 | 修復後（再実行）| 備考 |
|---|---|---|---|---|
| 1 | type-check | ❌ 失敗 | ✅ **成功** | 5/5 packages OK |
| 2 | lint | ❌ 失敗 | ⏸ 未対応 | ESLint 設定が未初期化。品質担保タスクとして残す |
| 3 | vitest (api) | ❌ 失敗（42/51）| ✅ **成功（51/51）** | 全 4 spec ファイル緑 |
| 4 | next build (web) | ❌ 失敗 | ✅ **成功** | 全ルート compile OK |
| 5 | Playwright E2E | ⏸ スキップ | ⏸ 未対応 | build が通るようになったので、次回実行可能 |

## 全体所見（暫定）

**テストコードと本番コードの schema drift が広範囲に存在**。「TODO マーカーが無く、20 ページが実装されている」という静的な観察は正しかったが、それらは **型検査すら通っていない状態** で放置されていた。特に：

- `apps/web/src/app/dashboard/profile/ProfileEditForm.tsx` はコンポーネント欠品 + フィールド名 drift で完全に壊れている（下記 §1 参照）
- API 側は、案件作成に `commitmentLevel` が必須追加されたが、テストが未更新（下記 §3 参照）
- User プロフィール更新は、テストが `name` を渡すが実装は `bio/genres/instruments` を渡す — スキーマの命名変更を追跡していない

---

## 詳細ログ

### 1. type-check — ❌ FAIL

- **成功**: `@creator-links/shared`, `@creator-links/api`
- **失敗**: `@creator-links/web` (7 エラー、全て 1 ファイル)

#### エラー詳細: `apps/web/src/app/dashboard/profile/ProfileEditForm.tsx`

| 行 | エラー | 内容 |
|---|---|---|
| 20 | TS2307 | `Cannot find module './PasswordChangeSection'` — ファイル自体が存在しない |
| 59 | TS2353 | フォーム defaultValues に `name` を指定しているが、スキーマ側は `displayName` |
| 143 | TS2345 | `handleSubmit` に渡す型が UpdateProfileInput と噛み合わない（同じ `name` vs `displayName` drift） |
| 147 | TS2345 | `register('name', ...)` 呼び出し — `name` はスキーマに存在しない |
| 150 | TS2339 | `errors.name` の参照 2 箇所 — 同上 |

**原因の推定**: プロフィール項目を `name` → `displayName` にリネームした際、`ProfileEditForm.tsx` の追従漏れ。加えて `PasswordChangeSection` はまだ実装されていない or 削除された。

**修復難度**: 低〜中（`name` → `displayName` の置換 + PasswordChangeSection の削除 or 実装）

### 2. lint — ❌ FAIL

`next lint` は Next.js 16 で deprecated 予告。ESLint の設定が未初期化状態で、対話プロンプト（"How would you like to configure ESLint?"）が出て CI/バッチ実行できない。

```
? How would you like to configure ESLint?
❯  Strict (recommended)
   Base
   Cancel
```

**修復難度**: 低（`npx @next/codemod@canary next-lint-to-eslint-cli .` で ESLint CLI へ移行 → 通常の `eslint .` に置き換え）

### 3. vitest (api) — ❌ FAIL

**結果: 51 テスト中 42 成功 / 9 失敗（3 ファイル失敗 / 1 ファイル完全成功）**

| ファイル | 結果 | 失敗数 |
|---|---|---|
| `match.test.ts` | ✅ | 0 / 12 |
| `schemas.test.ts` | ❌ | 7 / 22 |
| `user.test.ts` | ❌ | 1 / 8 |
| `project.test.ts` | ❌ | 1 / 9 |

#### 失敗詳細

**A. `schemas.test.ts`（7 失敗）**

全て「`safeParse().success` が期待通り true にならない」＝**スキーマ側の必須項目追加をテスト側が追えていない**。

- `CreateProjectSchema > スポット案件を正常に作成できる`
- `CreateProjectSchema > サブスクリプション案件を正常に作成できる`
- `ApplyMatchSchema > プロジェクトIDとメッセージで応募できる`
- `ApplyMatchSchema > メッセージなしでも応募できる`
- `CreateReviewSchema > 1〜5の評価を受け入れる`
- `CreateReviewSchema > コメント付きのレビューを受け入れる`
- `SendMessageSchema > 通常のメッセージを受け入れる`

**B. `project.test.ts`（1 失敗）**

- `project.create > 認証済みユーザーが案件を作成できる`
  - エラー: `commitmentLevel: Required ('HOBBY' | 'SEMI_PRO' | 'PRO')`
  - **原因**: `commitmentLevel` フィールドが必須になったが、テストのペイロードに無い

**C. `user.test.ts`（1 失敗）**

- `user.updateProfile > プロフィールを更新できる`
  - テストは `{ data: { name: '更新後の名前' } }` を期待
  - 実装は `{ data: { bio, genres, instruments }, omit: { passwordHash: true } }` を送信
  - **原因**: プロフィールモデルの再設計（`name` を廃し、`bio/genres/instruments` 中心へ）にテスト未追従

### 4. next build (web) — ❌ FAIL

```
./src/app/dashboard/profile/ProfileEditForm.tsx
Module not found: Can't resolve './PasswordChangeSection'
```

webpack のモジュール解決段階で即失敗し、それ以外のエラーは表面化しなかった。型検査で挙げた同じ根っこ（PasswordChangeSection 欠品）に阻まれている。**PasswordChangeSection を作るか import を削除しない限り本番 build は取れない。**

補足: `/dashboard/profile/page.tsx` は `ProfileEditForm` を直接 import しているため、`/dashboard/profile` ルート単体が完全に死んでいる。dev mode でも同ルートを踏むと webpack エラーで真っ白になる想定。

### 5. Playwright E2E — ⏸ スキップ

**スキップした理由:**

1. **build が通らない** — dev server は Next.js の lazy compile なので起動自体は可能だが、E2E で `/dashboard/profile` や関連ルートを踏んだ瞬間にコンパイルエラーで倒れる可能性が高い
2. **前提整備コストが大きい** — dev server 起動、seed 投入 (`apps/api/prisma/seed.ts`)、TEST_ACCOUNTS の状態確認、Playwright の browser install（未実施なら）で 5〜10 分は追加消費
3. **既に §1〜§4 で「体裁を確認する」目的は十分達成** — 修正必須の問題がすでに山積みで、E2E を走らせても新規シグナルは限定的（既知の broken 箇所に到達して失敗するだけの可能性が高い）

**E2E で本来カバーされる予定の導線:**

| ファイル | describe ブロック |
|---|---|
| `auth.spec.ts` | 認証フロー（ログイン/新規登録/リダイレクト） |
| `matching.spec.ts` | マッチング管理（発注者）/ チャット / ダッシュボード / アーティスト一覧 |
| `project.spec.ts` | 案件一覧 / 案件作成 / 案件詳細 |

**E2E を実行する場合の推奨手順（次回向け）:**

```bash
# ① seed 適用（初回のみ or drift 時）
pnpm --filter api db:seed

# ② Playwright browser install（初回のみ）
pnpm --filter web exec playwright install chromium

# ③ dev server（別ターミナル、compile 完了まで 30〜60 秒待つ）
pnpm --filter web dev

# ④ E2E 実行
pnpm --filter web test:e2e
```

---

## 全体所見（確定）

「20 ページが実装済で TODO マーカーも無い」＝ **静的コード上の体裁はある** は事実だが、**その体裁は型検査・ビルド・ユニットテストのいずれも通らない状態で凍結されている**。テストコードが本番コードの schema 変更（`name` → `displayName`、`commitmentLevel` 必須化、User update の payload 再設計）を追跡できておらず、直近の変更が既存の品質ゲートを壊したまま放置されている。

「決済以外の体裁は取れている」の解像度を上げると:

- ✅ ページ骨格・API route・server actions は存在する
- ✅ 主要導線（認証・案件・マッチ・チャット・プロフィール・お知らせ）のファイルは書かれている
- ❌ **型検査・ビルド・vitest のいずれも通らないので、書かれたコードが実際に動くかは未検証**
- ❌ `/dashboard/profile` は完全に壊れている（PasswordChangeSection 欠品でビルド不可）

したがって「決済以外は取れている」は「**土台の実装物はあるが、動作品質は未検証。手を入れる前にまず品質ゲートを緑にする必要がある**」と改める必要がある。

## 残タスク・課題

**優先度 高（修正しないと build も dev もまともに動かない）:**

1. **`ProfileEditForm.tsx` の再生** — `name` → `displayName` 置換 + `PasswordChangeSection` の去就決定（新設 or import 削除）
2. **`commitmentLevel` 必須化に伴うテスト更新** — `project.test.ts` の payload 追記
3. **`user.updateProfile` テストと実装の噛み合わせ** — `user.test.ts` を新スキーマ (`bio/genres/instruments`) に更新
4. **`schemas.test.ts` の 7 テスト** — 現行 Zod スキーマに合わせて payload 修正

**優先度 中（品質担保・CI 整備）:**

5. ESLint 設定の初期化（`next lint` deprecation 対応も兼ねる）
6. build が緑になったら E2E 実行 → 主要導線の実動作確認
7. type-check / vitest / build を CI で自動化して drift を早期検知

**優先度 低（今日の議論の延長）:**

8. Stripe 実装コードの除去計画（RevenueCat 全面化に伴う；memory `creatorlinks-stripe-escrow` 参照）
9. Prisma migration の baseline 化（本番前必須）

## 次のアクション提案

**A**: 上記「優先度 高」の 1〜4 をこの場で連続修正して、type-check / build / vitest を全て緑にする（推定所要 30〜60 分）
**B**: 修正は保留し、issue リストとして残す（マークダウンをそのまま参照ドキュメントに）
**C**: E2E だけ強行して追加シグナルを取りに行く（build が壊れているので追加情報は限定的、非推奨）

---

## 修復と再検証（2026-08-08 追記）

**選択: A（優先度 高 1〜4 を連続修正）**

### 実施した修正

**1. `ProfileEditForm.tsx` の再生**
- `import PasswordChangeSection from './PasswordChangeSection'` を削除（`/dashboard/account` 側に既存の実装があるため重複を除去）
- Props から `passwordSet: boolean` を削除
- フォーム項目 `name` → `displayName` に置換（UpdateProfileSchema と整合）
- 「名前 *」ラベルを「アーティスト表示名（未入力ならアカウント名がそのまま表示されます）」に変更
- placeholder に `user.name`（アカウント名）を出してフォールバック挙動を可視化
- 末尾のインライン `EmailChangeSection` を削除し、代わりに **`/dashboard/account` へのリンクカード** に差し替え（アカウント情報の変更導線を1箇所に集約）
- `page.tsx` は `displayName: true` を select に追加、`passwordHash` を drop、Props に合わせて整理

**2. `apps/api/src/__tests__/routers/user.test.ts`**
- `updateProfile` テストの payload / mock / 期待値を `name` から `displayName` に変更
- 期待値に `omit: { passwordHash: true }` を追加（router 実装と整合）

**3. `apps/api/src/__tests__/routers/project.test.ts`**
- `project.create` 両テストの payload に `commitmentLevel: 'HOBBY'` を追加

**4. `apps/api/src/__tests__/routers/schemas.test.ts`**
- CreateProjectSchema の 2 テストに `commitmentLevel` を追加
- ApplyMatchSchema / CreateReviewSchema / SendMessageSchema の 5 テストで、`projectId` / `matchId` の値を CUID 形式（`cltest00000000000000000001`）に変更

### 再実行結果

```
type-check:  Tasks: 5 successful, 5 total  (Time: 5.4s)
vitest (api): Test Files 4 passed (4)  Tests 51 passed (51)  (Duration 410ms)
next build:   全ルート compile OK（21 ルート）
```

`.next` に production build 成果物も生成された。

### 残タスク（今回の作業スコープ外）

**優先度 中:**
- ESLint 設定の初期化（`next lint` は Next.js 16 で deprecated；`npx @next/codemod@canary next-lint-to-eslint-cli .` で移行推奨）
- Playwright E2E の実行（build が緑になった今、seed + dev server 起動で試せる）
- type-check / vitest / build を CI で自動化して drift を早期検知

**優先度 低（別セッションの議論に紐づく）:**
- Stripe 実装コードの除去計画（RevenueCat 全面化に伴う；memory `creatorlinks-stripe-escrow` 参照）
- Prisma migration の baseline 化（本番前必須）

### 修復方針の記録

- `/dashboard/profile` はプロフィール（表示名・bio・ジャンル・熟練度・楽器・アバター・カバー）専用
- `/dashboard/account` はアカウント情報（アカウント名・メール・パスワード）専用
- パスワード変更・メール変更は `/dashboard/account` に集約し、プロフィール画面からはリンクで誘導する

この責務分離を今回明文化した。

---

# 2026-08-09 実装ログ（音楽業界特化ピボット + イベント/カレンダー機能追加）

対象: creatorLinks を音楽業界特化に振り直し、Phase A（イベント告知 + カレンダー）機能を実装。
ブランチ: `feat/phase-a-events-stripe-cleanup`
実行者: Claude Code (opus-4-7) + ユーザー本人（並行開発）

## ピボットの決定事項

### ブランド

- 旧: `CreatorLinks`（一般クリエイター向け）
- 新: **音楽業界特化**（マッチングアプリ + ミニ DX 機能）
- サービス名: 暫定 `Encore`（造語化予定・商標調査後に正式決定）
- 実装上はブランド名文字列を後日 1 箇所差し替えで済むよう整理予定

### ポジション

- 主軸: マッチング（既存路線維持）
- 追加軸: イベント告知・カレンダー・タスク管理を統合したミニ DX 機能
- 差別化: 「対面重視」「イベントが出会いのハブ」の業界文化に合わせた設計

### 収益方針（2026-08-09 撤回で確定）

一時的に「RevenueCat 全面統一」の方針で Stripe UI を停止したが、同日中に撤回：

- **Stripe** = 案件決済（P2P エスクロー）を維持
- **RevenueCat** = PRO サブスク決済
- 役割分離した二本柱で運用継続

## commit 履歴（このブランチで積んだ 5 コミット）

| commit | 内容 |
|---|---|
| `dfd0b85` | feat(events): Phase A 初期実装 + Stripe UI 除去（後で部分 revert） |
| `603f7b7` | revert(stripe): 方針転換に伴い Stripe UI/エスクローを部分復元 |
| `96560b0` | feat(events): Phase A 最小 UI（一覧/詳細/新規/カレンダー） |
| `ac9d92e` | feat(events): Phase A.5 visibility + タスク型統合 |
| `eca341d` | feat(follow): Phase A.6 SNS フォロー + アーティストページ拡張 |

## Phase A: イベント告知 + カレンダーの基礎（`96560b0`）

### 新規モデル（Prisma）

| モデル | 用途 |
|---|---|
| `Event` | 告知本体（LIVE/SESSION/RECORDING/WORKSHOP/MEETUP/OTHER） |
| `EventParticipant` | 主催者・出演者・スタッフ・オーディエンス |
| `EventOpenRole` | 公募枠（`Match.eventOpenRoleId` で応募が Match に接続） |
| `EventFollow` | イベント通知購読（Phase A.6 で概念整理） |
| `EventInterest` | ファンの「行く / 興味あり」自主表明 |

- `Match` に `eventOpenRoleId` カラム追加 → 応募・承認・完了・レビュー動線を Event でも再利用
- インデックス: `[status, startAt]`, `[creatorId, startAt]`, `[city, startAt]`

### tRPC ルーター

- `eventRouter`: create / update / publish / cancel / list / getById / invite / respondToInvite /
  removeParticipant / addOpenRole / updateOpenRole / closeOpenRole /
  applyToOpenRole / approveApplication / rejectApplication / followEvent /
  unfollowEvent / toggleInterest / clearInterest / myEvents
- `calendarRouter`: personal / public
- `apps/api/src/server/routers/index.ts` に登録

### shared パッケージ

- `packages/shared/src/types/event.ts` — EventType/EventParticipantRole/EventStatus/EventOpenRoleStatus 型 + ラベル
- `packages/shared/src/schemas/event.ts` — Zod スキーマ（CreateEventSchema / UpdateEventSchema /
  EventFilterSchema / InviteParticipantSchema / CreateOpenRoleSchema / ApplyToOpenRoleSchema など）

### フロント（最小 UI）

| パス | 内容 |
|---|---|
| `/events` | 公開イベント一覧（Server Component + Prisma 直参照） |
| `/events/new` | 作成フォーム（Client Component、Server Action 呼び出し） |
| `/events/[id]` | 詳細（開催情報・公募枠・出演者・応募/興味表明） |
| `/dashboard/calendar` | 個人カレンダー（月グリッド、ユーザー本人が並行 rewrite で Google 風に拡張） |

### Server Actions（`apps/web/src/server/actions/event.ts`）

- `createEventAction` / `publishEventAction` / `cancelEventAction`
- `toggleInterestAction` / `clearInterestAction`
- `applyToOpenRoleAction` / `addOpenRoleAction`

### サイドバー

- `sidebar-shell.tsx` に「イベント」「カレンダー」の nav item + `MicIcon` / `CalendarIcon` を追加

## Stripe 関連の紆余曲折

### 発生した問題

初回 commit `dfd0b85` で「RevenueCat 全面統一」の想定のもと以下を停止した：

- `apps/web/src/app/api/stripe/webhook/route.ts` → 501 stub
- `apps/web/src/app/api/cron/release-payments/route.ts` → 501 stub
- `apps/web/vercel.json` から release-payments cron 削除
- `apps/web/src/app/dashboard/payouts/page.tsx` → 「機能終了」ページ
- `apps/web/src/components/dashboard-shell/sidebar-shell.tsx` の「入金設定」nav 削除
- `apps/web/src/app/dashboard/chat/[id]/chat-client.tsx` の PaymentBadge / 支払う / 送金確認 UI 非表示
- `apps/web/src/app/projects/new/page.tsx` の予算ラベル・手数料 7% 削除
- `apps/web/src/components/landing/faq.tsx` の 7% 記述削除
- `apps/web/src/app/layout.tsx` description の 7% 削除
- `apps/web/src/server/actions/payments.ts` → no-op stub

### revert（`603f7b7`）で復元

方針撤回に伴い、以下を部分 revert：

- untracked-then-committed だった 4 ファイル (webhook / cron / payouts/page / payments.ts) を
  `git rm --cached` で index から外し、original content を復元（payments.ts は stub のまま）
- tracked-modified の Stripe 関連編集を Edit で reverse

### 保持した音楽業界向けブランディング変更（Stripe と無関係）

- OGP キーワードの音楽業界特化
- FAQ の職種列挙を音楽先頭

### ⚠️ 残タスク: `payments.ts` の完全復元

私（Claude）のセッションでは 200 行程度しか cache していないため、501 stub のまま残置。
並行セッションで動作する Stripe 実装の完全版で上書き必要。

## Phase A.5: visibility + タスク型統合（`ac9d92e`）

### 設計思想

「個人予定 / タスク管理 / 公開告知」を単一の Event モデルで扱う。
`status`（ライフサイクル）と `visibility`（誰に見えるか）を **直交する 2 軸** として設計。

### 追加モデル要素

```prisma
enum EventVisibility {
  PRIVATE            // 本人のみ
  PARTICIPANTS_ONLY  // 招待した共演者・スタッフ・出演者のみ
  FOLLOWERS          // フォロワー + 参加者
  PUBLIC             // 誰でも
}

model Event {
  // 既存...
  visibility      EventVisibility @default(PRIVATE)
  isAllDay        Boolean         @default(false)
  hasSpecificDate Boolean         @default(true)
}

enum EventType {
  // 既存: LIVE / SESSION / RECORDING / WORKSHOP / MEETUP / OTHER
  REHEARSAL // リハーサル・個人練習
  MEETING   // 打ち合わせ
  TODO      // タスク・やること
}
```

- 参加者（EventParticipant）は visibility に関わらず必ず閲覧可（絶対原則）
- インデックス: `@@index([status, visibility, startAt])`

### 用途マップ

| 用途 | 型 | 公開範囲 |
|---|---|---|
| 個人練習 | `REHEARSAL` | `PRIVATE` |
| バンド練習 | `REHEARSAL` | `PARTICIPANTS_ONLY` |
| やること | `TODO` | `PRIVATE` |
| ライブ告知 | `LIVE` | `PUBLIC` |
| フォロワー先行告知 | `LIVE` | `FOLLOWERS` |
| 打ち合わせ | `MEETING` | `PARTICIPANTS_ONLY` |

### tRPC / Server Action の変更

- `event.list`: `visibility=PUBLIC` のみ返す
- `event.getById`: visibility ベースの閲覧権限チェック
  （作成者・参加者は常時可、FOLLOWERS は Follow を確認）
- `calendar.public`: `visibility=PUBLIC` のみ
- `createEventAction`: visibility 受け取り、デフォルト `PRIVATE`
- `updateEventVisibilityAction` 新規追加（主催者のみ）

### shared

- `EVENT_VISIBILITIES` / `EVENT_VISIBILITY_LABELS` / `_DESCRIPTIONS` / `_ICONS` 追加
- `CreateEventSchema` に visibility / isAllDay / hasSpecificDate 追加

### UI

- `/events/new`: 公開範囲セレクタ（4 層 × 説明文）+ 終日トグル追加
  種別変更時にスマート提案（LIVE → PUBLIC、REHEARSAL / TODO → PRIVATE 等）
- `/events/[id]`: visibility バッジ表示

## Phase A.6: SNS フォロー + アーティストページ拡張（`eca341d`）

### 新規モデル

```prisma
model Follow {
  id           String   @id @default(cuid())
  followerId   String
  followingId  String
  createdAt    DateTime @default(now())

  @@unique([followerId, followingId])
  @@index([followingId])
}
```

- `EventFollow` は「通知購読」概念で残置（役割分離を明文化）
- `Follow` は「関係の宣言」= visibility=FOLLOWERS 判定 + プロフィール表示用

### tRPC

- `followRouter`: follow / unfollow / isFollowing / counts / followers / following
- `event.getById` の FOLLOWERS 判定を `Follow` に置換（`EventFollow` からの移行）

### Server Actions

- `followUserAction` / `unfollowUserAction`（optimistic update 対応）

### 新規ページ

| パス | 内容 |
|---|---|
| `/artists/[id]/events` | 主催・出演一覧、今後/過去 × 主催/出演/すべて フィルタ、visibility 対応 |
| `/artists/[id]/calendar` | 月グリッドカレンダー、視認性重視の UI |

### 既存プロフィール拡張

`/artists/[id]/page.tsx`:

- `FollowButton` コンポーネント（フォロー / フォロー中トグル）
- フォロワー数 badge（`FollowerCountBadge`）
- 「今後のイベント」プレビュー 3 件
- events / calendar への導線リンク

### 閲覧範囲ロジック（共通ルール）

| 閲覧者 | 見えるもの |
|---|---|
| 本人 | PRIVATE 含む全部 |
| フォロワー | PUBLIC + FOLLOWERS + 招待済 PARTICIPANTS_ONLY |
| 未フォロー（ログイン済） | PUBLIC + 招待済 PARTICIPANTS_ONLY |
| 未ログイン | PUBLIC のみ |

## 並行してユーザー本人が実施した拡張（別セッション）

- `/dashboard/calendar/page.tsx`: 素朴なリストから Google Calendar 風の月グリッドへ全面 rewrite
  - 6 週 × 7 曜のセル、当日ハイライト、前後月グレー
  - 各セルにロール色付き event pill、溢れは "+N 件"
  - 日番号クリック → `/events/new?date=YYYY-MM-DD` プリフィル導線
- `/events/new/page.tsx`: `?date=YYYY-MM-DD` query 対応、`NewEventForm` に `defaultDate` prop
- `/artists/[id]/page.tsx`: FollowButton 周りの UI 洗練（グラデーション、フォロワー数バッジのアイコン化）
- `follow-button.tsx`: フォロー中の状態表示、非ログイン向け badge のスタイル統一
- `landing/faq.tsx`: 音楽業界特化 FAQ に全面 rewrite、`Encore` 相性・「また一緒に」機能を予告

## 検証結果（このブランチ）

| 項目 | 結果 |
|---|---|
| Prisma schema validation | ✅ 通過 |
| DB schema sync (`prisma db push`) | ✅ 適用済み（本番前 baseline 化は別途必要） |
| api type-check | ✅ 通過 |
| shared build | ✅ 通過 |
| web type-check | ✅ 通過 |
| api vitest（既存 51 テスト） | ✅ Phase A の commit 前で 51/51 パス確認済 |
| E2E | ⏸ 未実施 |

## 詰まったポイント（次セッションのため）

### 1. Prisma Client と dev server のキャッシュ

Prisma スキーマ変更 → `db push` を打っても、走っている dev server（`next dev` + `tsx watch`）は
古い Client を Node.js プロセスにキャッシュしている。**dev server 再起動が必須**。

対処：
```bash
lsof -ti :3001 | xargs kill -9 2>/dev/null
lsof -ti :3000 | xargs kill -9 2>/dev/null
pkill -f "tsx watch" 2>/dev/null
sleep 1
pnpm --filter api exec prisma generate
pnpm dev
```

Runtime エラー "Cannot read properties of undefined (reading 'findMany' / 'count')" が出たら
必ず dev server 再起動から確認。

### 2. Prisma migration の drift

DB は `db push` で継続的に更新しているが、`prisma/migrations/` には migration file が存在しない
（過去に db push ベースで運用されてきた）。本番デプロイ前に baseline 化が必須：

```bash
pnpm --filter api exec prisma migrate diff \
  --from-empty --to-schema-datamodel prisma/schema.prisma \
  --script > prisma/migrations/0_init/migration.sql
pnpm --filter api exec prisma migrate resolve --applied 0_init
```

### 3. 未追跡ファイルと commit のミスマッチ

`payments.ts` 等の Stripe 実装ファイルはこれまで git 管理外の untracked WIP だった。
初回 commit `dfd0b85` で誤って git 追加してから内容を stub 化してしまい、original content が git
にも Read キャッシュにも完全な形で残っていない状態が発生した。
→ 復元は `git rm --cached` + 部分的な Write で対応、payments.ts は並行セッションでの完全復元待ち。

### 4. Complex Prisma where と TypeScript 型推論

`where: { AND: [...] }` に Union 型の変数を渡すと Prisma の返り値型から `include` した relation が
消える。対処：

```typescript
import type { Prisma } from '@prisma/client'
const where: Prisma.EventWhereInput = { AND: [...] }
```

## 残タスク・課題

### 優先度 高

1. **`apps/web/src/server/actions/payments.ts` の Stripe 実装完全復元** ← 並行セッション対応
2. **Prisma migration baseline 化**（本番デプロイ前必須）
3. **dev server 再起動フロー**を README に明記（Prisma スキーマ変更時の hard reset 手順）

### 優先度 中（次に着手候補）

4. 通知の visibility 別発火（新規 FOLLOWERS イベント時にフォロワーへ Push）
5. visibility 昇格・降格 UI（詳細ページで後から変更）
6. Follow 通知（フォローされた側への通知）
7. フォロワー向けフィード（新規タイムライン）
8. アーティスト一覧に「今週ライブあり」バッジ

### 優先度 低

9. サブタスク・チェックリスト・繰り返し（Event Phase B 機能）
10. ブランド名「Encore」の商標・ドメイン調査 → 造語化 → 一括差し替え
11. ESLint 設定初期化（Next.js 16 の `next lint` deprecated 対応）
12. Playwright E2E の初回実行
